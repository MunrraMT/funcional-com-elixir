IO.puts("Content-Type: " <> content_type = "Content-Type: text/html; charset=UTF-8")
IO.puts(content_type)
# Adiciona à variável "content_type" a string sem o conteúdo da string que fez match
# retornando:
# text/html; charset=UTF-8
